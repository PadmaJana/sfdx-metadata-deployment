name: Validate Metadata

on:
  push:
    branches:
      - dev

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      # 1️⃣ Checkout repo
      - name: Checkout Repo
        uses: actions/checkout@v3

      # 2️⃣ Install Salesforce CLI (sfdx)
      - name: Install Salesforce CLI
        run: npm install sfdx-cli --global

      # 3️⃣ Authenticate DevSandbox using SFDX Auth URL
      - name: Authenticate DevSandbox
        shell: bash
        run: |
          # Save secret to file, remove Windows carriage returns
          echo "${{ secrets.DEV_SFDX_AUTH_URL }}" | tr -d '\r' > sfdx_auth.txt
          
          # Authenticate and set alias & default username
          sfdx auth:sfdxurl:store -f sfdx_auth.txt --alias DevSandbox --setdefaultusername
          
          # Verify immediately in the same shell
          sfdx force:org:display -u DevSandbox

      # 4️⃣ Validate Metadata Deployment
      - name: Validate Metadata Deployment
        run: |
          sfdx force:source:deploy \
          -p force-app/main/default,core/main/default,utility/main/default \
          -u DevSandbox \
          --checkonly \
          --ignorewarnings \
          --json

      # 5️⃣ Send Email on Failure
      - name: Send Email on Validation Failure
        if: failure()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "🚨 Validation Failure on branch ${{ github.ref }}"
          to: apadmajan650@gmail.com
          from: ci-cd@yourcompany.com
          body: |
            Validation failed on branch ${{ github.ref }}.
            Check logs here: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
