name: Deploy to Dev

on:
  push:
    branches:
      - dev

jobs:
  deploy-dev:
    runs-on: ubuntu-latest
    steps:
      # 1️⃣ Checkout the repo
      - name: Checkout Repo
        uses: actions/checkout@v3

      # 2️⃣ Install Salesforce CLI
      - name: Install Salesforce CLI
        run: npm install sfdx-cli --global

      # 3️⃣ Authenticate DevSandbox properly
      - name: Authenticate Dev Org
        run: |
          # Remove any carriage returns or extra spaces
          echo "${{ secrets.DEV_SFDX_AUTH_URL }}" | tr -d '\r' > sfdx_auth.txt
          
          # Authenticate and set default username for all steps
          sfdx auth:sfdxurl:store -f sfdx_auth.txt --setdefaultusername -a DevSandbox

      # 4️⃣ Verify authentication
      - name: Verify DevSandbox
        run: sfdx force:org:display -u DevSandbox

      # 5️⃣ Deploy metadata
      - name: Deploy Metadata to Dev
        run: |
          sfdx force:source:deploy \
          -p force-app/main/default,core/main/default,utility/main/default \
          -u DevSandbox \
          --testlevel RunLocalTests \
          --wait 10

      # 6️⃣ Send failure email
      - name: Send Email on Failure
        if: failure()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "🚨 Salesforce Deployment Failure on branch ${{ github.ref }}"
          to: apadmajan650@gmail.com
          from: ci-cd@yourcompany.com
          body: |
            Deployment to DevSandbox failed.
            Check logs: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
